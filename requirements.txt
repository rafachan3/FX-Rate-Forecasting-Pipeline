# Production requirements for FX Rate Forecasting Pipeline
# 
# To update deterministically:
# 1. Install packages: python -m pip install -r requirements.txt
# 2. After adding/removing packages, run: python -m pip freeze | grep -E "(numpy|pandas|pyarrow|scikit-learn|joblib|pytest|matplotlib|jupyter|ipykernel|xgboost)" > requirements_new.txt
# 3. Replace version ranges with exact pins (==) and verify tests pass: pytest -q
#
# Runtime dependencies (required for production code)
numpy==2.3.5
pandas==2.3.3
pyarrow==22.0.0
scikit-learn==1.8.0
joblib==1.5.3
boto3==1.35.0

# Development and testing dependencies
pytest==9.0.2

# Notebook dependencies (optional, for local development/analysis)
matplotlib==3.10.8
jupyter==1.1.1
ipykernel==7.1.0
xgboost==3.1.2

# Email dependencies
sendgrid==6.12.5